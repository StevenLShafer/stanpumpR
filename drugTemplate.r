drugTemplate <- function(weight, height, age, sex)
{
  # Units **************
  # Time: Minutes
  # Volume: Liters
  
  # Add pK here
  
  return(
    list(
      v1 = v1, 
      v2 = v2, 
      v3 = v3, 
      cl1 = cl1, 
      cl2 = cl2, 
      cl3 = cl3,
      tPeak = tPeak, 
      MEAC = MEAC, 
      typical = typical, 
      upperTypical = upperTypical, 
      lowerTypical = lowerTypical, 
      reference = reference
    )
  )
}    
