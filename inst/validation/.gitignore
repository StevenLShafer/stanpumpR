# Deterministically regenerated by gasman_export_results.R from committed code
# and committed scenario inputs, so the binary is not tracked. VALIDATION.md
# records the commit each sent workbook was generated from.
gasman_baseline_results.xlsx
gasman_baseline_summary.xlsx

# Gas Man's own CSV exports land here when comparing; they are Rich's outputs,
# not ours, and are recorded in VALIDATION.md rather than tracked wholesale.
gasman_out/
